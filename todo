- adding/removing residencies
    add button to add user to unit (adding a residency) to units that are vacant
    add button to remove user (delete residency) to units that are occupied

- set default value of manager status to false!
- set default value of start_date, and curr_balance for residency?

- create a model called payment
    residencies have many payments
    resdiency calculates current balance from
        a. how many months have passed since start date
        b. how many payments have been made

- style app with bootstrap!